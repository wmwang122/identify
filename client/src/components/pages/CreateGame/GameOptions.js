const GameOptions 
import RoomNameInput from "./RoomNameInput.js"

export default GameOptions = () => {
    
return(
    /* <> room name >?
    <> num questions <>
    <> genre <> 

    <>after creating a room, adds game room to database<>
    return (
        "string"
    );*/
    ""
)}

const GameOptions 
